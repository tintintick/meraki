<style>
  #shopify-section-{{ section.id }} {
    background-color: #fcfbf7;
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
   @media (max-width: 46.875rem) {
    #shopify-section-{{ section.id }} {
      padding-top: {{ section.settings.padding_top_mobile }}px;
      padding-bottom: {{ section.settings.padding_bottom_mobile }}px;
    }
  }
</style>
<div class="main-brand-container container-default">
  <div class="main-brand-content">
    <!-- Left side - Image carousel with thumbnails -->
    <div class="main-brand-image-section">
      <!-- Thumbnails - Left side vertical -->
      <main-brand-thumbnails class="main-brand-thumbnails">
        {% for block in section.blocks %}
          {% if block.type == 'brand_media_item' and block.settings.image %}
            <div
              class="main-brand-thumbnail {% if forloop.first %}active{% endif %}"
              data-index="{{ forloop.index0 }}"
            >
              {{
                block.settings.image
                | image_url: width: 200
                | image_tag: loading: 'lazy', alt: block.settings.image.alt
              }}
            </div>
          {% endif %}
        {% endfor %}
      </main-brand-thumbnails>

      <!-- Main carousel area -->
      <div class="main-brand-carousel-area">
        <main-brand-content-container class="main-brand-content-container">
          <div class="main-brand-carousel">
            <div class="main-brand-carousel-inner">
              {% for block in section.blocks %}
                {% if block.type == 'brand_media_item' and block.settings.image %}
                  <div class="main-brand-carousel-slide">
                    <div class="main-brand-main-image">
                      {{
                        block.settings.image
                        | image_url: width: 600
                        | image_tag: loading: 'lazy', alt: block.settings.image.alt
                      }}
                    </div>
                  </div>
                {% endif %}
              {% endfor %}
            </div>
          </div>
        </main-brand-content-container>
        <!-- Navigation arrows -->
        <main-brand-slide-container-arrow class="main-brand-arrows">
          <button class="main-brand-arrow-left" type="button" aria-label="Previous image">
            {{ 'icon-arrow-left.svg' | inline_asset_content }}
          </button>
          <button class="main-brand-arrow-right" type="button" aria-label="Next image">
            {{ 'icon-arrow-right.svg' | inline_asset_content }}
          </button>
        </main-brand-slide-container-arrow>

        <!-- Carousel dots -->
        <main-brand-dots class="main-brand-dots">
          {% for block in section.blocks %}
            {% if block.type == 'brand_media_item' and block.settings.image %}
              <div
                class="main-brand-dot {% if forloop.first %}active{% endif %}"
                data-dot-index="{{ forloop.index0 }}"
              ></div>
            {% endif %}
          {% endfor %}
        </main-brand-dots>
      </div>
    </div>

    <!-- Right side - Content -->
    <div class="main-brand-content-section">
      <h2 class="main-brand-title">{{ section.settings.title }}</h2>

      <div class="main-brand-description">
        {{ section.settings.description }}
      </div>

      {% if section.settings.button_text %}
        <a href="{{ section.settings.button_link }}" class="main-brand-cta-button" target="_blank">
          {{ section.settings.button_text }}
        </a>
      {% endif %}

      {% if section.settings.region %}
        <div class="main-brand-shipping-regions">
          {{ section.settings.region }}
        </div>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Main Brand",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Meraki x Dark Horse"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description",
      "default": "<p>Founded in a small San Diego neighborhood in California, USA, in 2013, Dark Horse Coffee has grown from a cold brew bar into a roastery with locations in San Diego and Kauai, Hawaii, emphasizing quality, transparency, and care.</p><p>Dark Horse is committed to close, long-term relationships with smallholder farmers around the world, guided by direct trade sourcing principles of trust, accountability, and a deep respect for every hand involved.</p><p>We are excited to feature Dark Horse as a CoffeeSense™ Partner. Paired with the precision of Meraki machines, their roasts come to life exactly as intended, offering an experience that feels as honest as it tastes.</p>"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "Shop Dark Horse Beans"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "richtext",
      "id": "region",
      "label": "Shipping regions",
      "default": "<p>Australia, Canada, Ireland, Netherlands, Puerto Rico, U.S. Virgin Islands, United States</p>"
    },
    {
      "type": "header",
      "content": "spacing"
    },
    {
      "type": "number",
      "id": "padding_top",
      "label": "Padding Top",
      "default": 60
    },
    {
      "type": "number",
      "id": "padding_bottom",
      "label": "Padding Bottom",
      "default": 60
    },
    {
      "type": "number",
      "id": "padding_top_mobile",
      "label": "Padding Top Mobile",
      "default": 16
    },
    {
      "type": "number",
      "id": "padding_bottom_mobile",
      "label": "Padding Bottom Mobile",
      "default": 64
    }
  ],
  "blocks": [
    {
      "type": "brand_media_item",
      "name": "Brand Media Item",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Main Brand",
      "blocks": [
        {
          "type": "brand_media_item"
        },
        {
          "type": "brand_media_item"
        }
      ]
    }
  ]
}
{% endschema %}
